<?xml version="1.0"?>
<Container version="2">
  <Name>shopping-hub</Name>
  <Repository>allornothing/shopping-hub:latest</Repository>
  <Registry>https://hub.docker.com/r/allornothing/shopping-hub</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>bash</Shell>
  <Privileged>false</Privileged>

  <Support>https://github.com/aon082910/shopping-hub/issues</Support>
  <Project>https://github.com/aon082910/shopping-hub</Project>
  <TemplateURL>https://raw.githubusercontent.com/aon082910/shopping-hub/main/templates/shopping-hub.xml</TemplateURL>
  <ReadMe>https://raw.githubusercontent.com/aon082910/shopping-hub/main/README.md</ReadMe>
  <Icon>https://raw.githubusercontent.com/aon082910/shopping-hub/main/icon.png</Icon>
  <WebUI>http://[IP]:[PORT:8000]/</WebUI>

  <Overview>
Cross-marketplace product aggregation and price comparison.

Crawls AliExpress, Alibaba, DHgate, Banggood, Made-in-China, Geekbuying, Chinavasion
and eBay, works out which listings are the same physical product, and publishes one
item page per product showing every site's price, minimum order quantity and
disclosed shipping - translated to English, with forwarding-agent links for the sites
that will not ship to the US.

Products are matched on five signals - perceptual image hash, normalised title,
brand, manufacturer part number and extracted specs - rather than titles alone, which
are close to useless on these sites.

Comparison is done on landed cost (unit price x minimum order quantity + freight +
duty), because the cheapest unit price often belongs to the most expensive order. A
US retail baseline from eBay is included, so the tool can tell you when importing is
not actually cheaper.

Also included: reverse image search, bulk/BOM costing, break-even analysis, price and
restock alerts, supplier trust scoring, category browsing and full-text search.

TWO SETTINGS THAT MATTER:

1. Set SOURCEHUB_ADMIN_TOKEN below. The admin pages are disabled without it, by
design - container traffic never looks like loopback to the app, so leaving it unset
would otherwise expose an unauthenticated admin panel to your whole LAN. Sign in with
the token as the password (any username). Browsing, search and crawling work either
way.

2. Leave Extra Parameters set to --shm-size=1g. Docker's default 64MB /dev/shm makes
Chromium crash on heavy pages, and it fails silently - the browser-rendered sites just
return zero listings.

Everything lives under the single /config mount: database, images, browser profile and
the editable YAML config. Back that up and you have backed up the app.
  </Overview>

  <Category>Productivity: Tools:Utilities</Category>
  <ExtraSearchTerms>shopping price comparison aliexpress alibaba dhgate banggood ebay scraper dropshipping sourcing importing</ExtraSearchTerms>
  <Requires>Internet access. A residential proxy is recommended for sustained crawling.</Requires>
  <Changes>
### 1.0.0
Initial release. Adapters for AliExpress, Alibaba, DHgate, Banggood, Made-in-China,
Geekbuying, Chinavasion and eBay. Landed-cost comparison, break-even analysis,
reverse image search, BOM costing, price/restock watches, full-text search.
  </Changes>
  <Date>2026-08-31</Date>
  <License>MIT</License>
  <MinVer>6.9</MinVer>

  <ExtraParams>--shm-size=1g</ExtraParams>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>

  <Config Name="WebUI Port" Target="8000" Default="8000" Mode="tcp"
          Description="Port for the web interface."
          Type="Port" Display="always" Required="true" Mask="false">8000</Config>

  <Config Name="Appdata" Target="/config" Default="/mnt/user/appdata/shopping-hub" Mode="rw"
          Description="Database, downloaded images, browser login profile and the editable YAML config all live here. Back this up."
          Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/shopping-hub</Config>

  <Config Name="Admin Token" Target="SOURCEHUB_ADMIN_TOKEN" Default="" Mode=""
          Description="REQUIRED to use the admin pages in a container. Any strong random string. Sign in with it as the password (any username). Leave blank and admin returns 403 - browsing and search still work."
          Type="Variable" Display="always" Required="false" Mask="true"/>

  <Config Name="Mode" Target="SOURCEHUB_MODE" Default="serve" Mode=""
          Description="serve = web UI plus scheduled crawling (recommended). web = UI only, no crawling. schedule = crawler only, no UI."
          Type="Variable" Display="always" Required="false" Mask="false">serve</Config>

  <Config Name="TZ" Target="TZ" Default="America/New_York" Mode=""
          Description="Timezone. The crawl schedule in config.yaml is interpreted in this zone."
          Type="Variable" Display="always" Required="false" Mask="false">America/New_York</Config>

  <Config Name="Translation Provider" Target="TRANSLATE_PROVIDER" Default="none" Mode=""
          Description="claude, deepl, google_free or none. Without translation, Chinese listings cannot be title-matched and mostly stay on their own product pages."
          Type="Variable" Display="always" Required="false" Mask="false">none</Config>

  <Config Name="Anthropic API Key" Target="ANTHROPIC_API_KEY" Default="" Mode=""
          Description="Used when Translation Provider is set to claude. Translations are cached by content hash, so a repeat crawl costs nothing."
          Type="Variable" Display="always" Required="false" Mask="true"/>

  <Config Name="PUID" Target="PUID" Default="99" Mode=""
          Description="User ID. 99 (nobody) is the Unraid default and keeps appdata editable from the host."
          Type="Variable" Display="advanced" Required="false" Mask="false">99</Config>

  <Config Name="PGID" Target="PGID" Default="100" Mode=""
          Description="Group ID. 100 (users) is the Unraid default."
          Type="Variable" Display="advanced" Required="false" Mask="false">100</Config>

  <Config Name="eBay Client ID" Target="EBAY_CLIENT_ID" Default="" Mode=""
          Description="Free from developer.ebay.com. Without it eBay falls back to browser scraping, which works but is slower."
          Type="Variable" Display="advanced" Required="false" Mask="false"/>

  <Config Name="eBay Client Secret" Target="EBAY_CLIENT_SECRET" Default="" Mode=""
          Description="Free from developer.ebay.com."
          Type="Variable" Display="advanced" Required="false" Mask="true"/>

  <Config Name="Proxy" Target="SOURCEHUB_PROXY" Default="" Mode=""
          Description="Optional. One http://user:pass@host:port, or several comma-separated for round-robin. Strongly recommended for sustained crawling."
          Type="Variable" Display="advanced" Required="false" Mask="true"/>

  <Config Name="CN Provider Key" Target="CN_PROVIDER_KEY" Default="" Mode=""
          Description="Optional API key for Taobao/Tmall/1688 data. Without it those three need a one-time browser login, which is awkward in a container."
          Type="Variable" Display="advanced" Required="false" Mask="true"/>
</Container>
